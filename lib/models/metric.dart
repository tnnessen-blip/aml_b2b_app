class Metric {
  const Metric(this.label, this.value, this.delta, this.detail, this.tone);

  final String label;
  final String value;
  final String delta;
  final String detail;
  final String tone;
}
